spring.application.name=sedentarismo-api
server.port=8080

# Sidecar Python (FastAPI) que serve o pipeline joblib treinado no notebook.
# Quando indisponível, o backend cai automaticamente nas regras explicáveis.
model.service.base-url=http://localhost:8000
model.service.timeout-ms=2500
